spring:
  datasource:
    url: jdbc:mysql://localhost:3306/msv_invoice_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root123

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.msv.invoiceservice: DEBUG
    org.hibernate.SQL: DEBUG